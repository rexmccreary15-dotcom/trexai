# Download Node.js - Direct Links (No Sign-In Required)

## 🚀 EASIEST METHOD: Direct Download Links

Just copy and paste these links directly into your browser - no sign-in needed!

### Latest LTS Version (v20.11.0) - Recommended

**For 64-bit Windows (Most computers):**
```
https://nodejs.org/dist/v20.11.0/node-v20.11.0-x64.msi
```

**For 32-bit Windows (Older computers):**
```
https://nodejs.org/dist/v20.11.0/node-v20.11.0-x86.msi
```

### How to Use:
1. **Copy** one of the links above (the 64-bit one if you're not sure)
2. **Paste** it into your browser's address bar
3. **Press Enter**
4. The download will start automatically
5. **Run** the downloaded file (`node-v20.11.0-x64.msi`)
6. **Click "Next"** through the installer (keep all defaults)
7. **Restart your computer** after installation

---

## 🔧 Alternative: Use Winget (Windows Package Manager)

If you have Windows 10/11, try this:

1. **Press `Windows Key`** and type **"PowerShell"**
2. **Right-click** on "Windows PowerShell" 
3. **Click "Run as Administrator"**
4. **Copy and paste** this command:

```powershell
winget install OpenJS.NodeJS.LTS
```

5. **Press Enter** and wait for it to install
6. **Restart your computer**

---

## 📥 Alternative: Download from GitHub

Since you have a GitHub account:

1. **Go to**: https://github.com/nodejs/node/releases
2. **Look for** the latest release with "LTS" tag (currently v20.11.0)
3. **Scroll down** to "Assets"
4. **Click** `node-v20.11.0-x64.msi` to download
5. **Run** the downloaded file
6. **Install** and restart

---

## ✅ Verify It Worked

After restarting your computer:

1. **Open PowerShell** (regular, not admin)
2. **Type**: `node --version`
3. **Press Enter**

You should see: `v20.11.0` (or similar)

If you see a version number ✅ **SUCCESS!**

---

## 🎯 Next Steps

Once Node.js is installed:

1. **Open PowerShell**
2. **Run**:
```powershell
cd "c:\Users\Rexan\.cursor\cursor app"
npm install
npm run dev
```

3. **Open browser**: http://localhost:3000

---

## 💡 Quick Tip

The **direct download link method** (first one) is usually the fastest and doesn't require any accounts!
